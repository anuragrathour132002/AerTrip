import CardResults from "../shared/ResultCards"
import SearchHeader from "../shared/SearchHeader"

const Flights = () => {
  return (
    <div className=''>
      <SearchHeader />
      <CardResults />
    </div>
  )
}

export default Flights